<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Purple Loan EMI Calculator</title>
<style>
    body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        background: linear-gradient(135deg, #6a0dad, #b56cf0);
        font-family: "Poppins", sans-serif;
    }

    .container {
        width: 380px;
        padding: 25px;
        border-radius: 18px;
        background: #ffffff;
        box-shadow: 0 8px 25px rgba(0,0,0,0.2);
        animation: fadeIn 0.8s ease-in-out;
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }

    h2 {
        text-align: center;
        color: #5c00a3;
        margin-bottom: 20px;
        font-size: 26px;
        font-weight: 700;
    }

    label {
        font-weight: 600;
        color: #4b0078;
        display: block;
        margin-bottom: 6px;
        font-size: 15px;
    }

    input {
        width: 100%;
        padding: 12px;
        font-size: 16px;
        margin-bottom: 15px;
        border: 2px solid #c5a3df;
        border-radius: 10px;
        outline: none;
        transition: 0.3s;
    }

    input:focus {
        border-color: #7d2ae8;
        box-shadow: 0 0 5px rgba(125,42,232,0.5);
    }

    button {
        width: 100%;
        padding: 12px;
        font-size: 18px;
        background: linear-gradient(to right, #7d2ae8, #a162ff);
        color: white;
        font-weight: bold;
        border: none;
        border-radius: 10px;
        cursor: pointer;
        transition: 0.3s;
        margin-top: 5px;
    }

    button:hover {
        opacity: 0.9;
        transform: scale(1.02);
    }

    .result-box {
        margin-top: 15px;
        background: #f3e8ff;
        border-left: 6px solid #7d2ae8;
        padding: 12px;
        border-radius: 10px;
        font-size: 16px;
        line-height: 1.5;
        color: #3b0070;
        font-weight: 600;
    }
</style>
</head>
<body>

<div class="container">
    <h2>Loan EMI Calculator</h2>

    <label>Loan Amount (₹)</label>
    <input type="number" id="loanAmount" placeholder="Enter Loan Amount">

    <label>Annual Interest Rate (%)</label>
    <input type="number" id="interestRate" placeholder="Enter Interest Rate">

    <label>Tenure (Years)</label>
    <input type="number" id="loanTenure" placeholder="Enter Tenure in Years">

    <button onclick="calculateEMI()">Calculate EMI</button>

    <div id="resultSection" style="display:none;">
        <div class="result-box" id="loan-result"></div>
        <div class="result-box" id="interest-result"></div>
        <div class="result-box" id="emi-result"></div>
        <div class="result-box" id="repayment-result"></div>
    </div>
</div>

<script>
function calculateEMI() {
    let P = parseFloat(document.getElementById("loanAmount").value);
    let annualRate = parseFloat(document.getElementById("interestRate").value);
    let years = parseInt(document.getElementById("loanTenure").value);

    if (!P || !annualRate || !years) {
        alert("⚠ Please fill all inputs correctly!");
        return;
    }

    let R = annualRate / 12 / 100;  
    let N = years * 12;

    let EMI = P * R * Math.pow((1 + R), N) / (Math.pow((1 + R), N) - 1);
    let totalPayment = EMI * N;
    let totalInterest = totalPayment - P;

    document.getElementById("loan-result").innerHTML =
        "Loan Amount: ₹ " + P.toLocaleString();
    document.getElementById("interest-result").innerHTML =
        "Total Interest: ₹ " + totalInterest.toLocaleString(undefined,{maximumFractionDigits:0});
    document.getElementById("emi-result").innerHTML =
        "Monthly EMI: ₹ " + EMI.toLocaleString(undefined,{maximumFractionDigits:0});
    document.getElementById("repayment-result").innerHTML =
        "Total Repayment: ₹ " + totalPayment.toLocaleString(undefined,{maximumFractionDigits:0});

    document.getElementById("resultSection").style.display = "block";
}
</script>

</body>
</html>
